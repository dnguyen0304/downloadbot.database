ARG DOMAIN
ARG PACKAGE_NAME
ARG BASE_IMAGE_VERSION
FROM ${DOMAIN}/${PACKAGE_NAME}-runtime:${BASE_IMAGE_VERSION}

MAINTAINER Duy Nguyen <dnguyen0304@gmail.com>

ENTRYPOINT python -c "import nose; nose.main()"
